<Window x:Class="AppAdmin.Views.ChangeInfoTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppAdmin.Views"
        mc:Ignorable="d"
        Title="Change" Height="130" Width="360" WindowStartupLocation="CenterOwner">
    <Grid  >

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="tbChangeName" HorizontalAlignment="Left" Margin="10,16,0,0" 
                     TextWrapping="Wrap" VerticalAlignment="Top" Width="250" Grid.Column="1" Height="18"/>
            <Label Content="Название" HorizontalAlignment="Left" Margin="10,12,0,0" 
                   VerticalAlignment="Top" Height="26" Width="63"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Время на тест(сек):" HorizontalAlignment="Center"
                 VerticalAlignment="Top" Height="26" FontSize="10" Width="98"/>
            <TextBox x:Name="tbTime" HorizontalAlignment="Left" Margin="10,0,0,0" PreviewTextInput="NumberValidationTextBox" 
                TextWrapping="Wrap" VerticalAlignment="Top" Width="210" Grid.Column="1" Height="18"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btAdd" Content="OK" HorizontalAlignment="Center"  VerticalAlignment="Center" Grid.Column="0"
          Margin="10,0,0,0"  Click="btAdd_Click" Width="100" Height="20" Grid.Row="1"/>

            <Button x:Name="btCancel" Content="Отменить" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="1"
          Margin="10,0,0,0"  Click="btCancel_Click" Width="100" Height="20" Grid.Row="1"/>
        </Grid>

    </Grid>
</Window>