<UserControl x:Class="AppAdmin.TestsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppAdmin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="130*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}"/>
        </Grid.Background>
        <Grid Margin="0,0,0,45">
            <ListBox x:Name="TestList" SelectionChanged="TestList_SelectionChanged">
                <Button Content="Добавить" Click="Button_Click"/>
            </ListBox>
        </Grid>
        <StackPanel x:Name="pnTestList" Grid.Column="1" Grid.ColumnSpan="2"/>
        <Button x:Name="btAddQuestion" Grid.Column="1" Content="Добавить вопрос" HorizontalAlignment="Left" Margin="25,10,0,0" VerticalAlignment="Top" Width="115" Click="btAddQuestion_Click"/>

    </Grid>
</UserControl>
